{
  "name": "codehustla",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm start --prefix packages/api",
    "server": "npm run dev --prefix packages/api",
    "client": "npm start --prefix packages/app",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "prod": "NODE_ENV=production npm run server",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix packages/app && npm run build --prefix packages/app"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "4.1.0"
  }
}
